---
title: Connect an AuthentikInstance
description: Point the operator at a real Authentik server.
---

Every other CRD in this operator references an `AuthentikInstance` by
name (`spec.instanceRef`, or implicitly for cluster-scoped kinds — see
the [allow-list guide](/docs/guides/allow-list) and the
[CRD reference](/docs/crds/authentikinstance)). `AuthentikInstance`
itself has no corresponding object on the Authentik side — it just
describes how to reach one.

## 1. Create a Secret with the API token

Generate an API token in Authentik (**Admin interface → Directory →
Tokens**, or via a service account), then:

```bash
kubectl create secret generic authentik-api-token \
  --namespace weebo-authentik \
  --from-literal=token=<your-api-token>
```

## 2. Create the AuthentikInstance CR

```yaml
apiVersion: authentik.weebo.io/v1alpha1
kind: AuthentikInstance
metadata:
  name: main
spec:
  url: https://login.example.com
  tokenSecretRef:
    name: authentik-api-token
    namespace: weebo-authentik
    key: token
```

`AuthentikInstance` is cluster-scoped, so its name (`main` above) is
what every `AuthentikApplication`'s `spec.instanceRef` points back to.

`spec.url` is the **web** base URL, not the `/api/v3` REST endpoint: the
operator appends `/api/v3` itself for API calls, and uses the bare URL as the
root of the per-application OIDC issuer it writes into an oauth2
application's `AUTHENTIK_URL` (`<url>/application/o/<slug>/`). A URL that
already ends in `/api/v3` is accepted and trimmed back to the web base rather
than taken literally.

## TLS

The operator verifies Authentik's certificate against the platform trust
store (plus `SSL_CERT_FILE`/`SSL_CERT_DIR`, if you'd rather inject the CA
into the pod's own trust store). When Authentik is served by a private CA
— a cluster-internal PKI, cert-manager's own CA issuer — point
`spec.tls.caSecretRef` at a Kubernetes `Secret` holding that CA's PEM
bundle instead:

```yaml
apiVersion: authentik.weebo.io/v1alpha1
kind: AuthentikInstance
metadata:
  name: main
spec:
  url: https://login.example.com
  tokenSecretRef:
    name: authentik-api-token
    namespace: weebo-authentik
    key: token
  tls:
    caSecretRef:
      name: authentik-ca
      namespace: weebo-authentik
      key: ca.crt # must hold a PEM bundle
```

The operator reads the Secret via the API and hands the PEM to the HTTP
client it talks to this instance with — no volume mount, which matters
because the chart exposes `podAnnotations` and `extraEnv` but no
`extraVolumes`. The CA is added *on top of* the platform trust store
rather than replacing it, so one operator can serve a private-CA instance
and a publicly-signed one at once. A single certificate and a concatenated
chain are both accepted.

The Secret is re-read on every reconcile, so rotating the CA takes effect
without restarting the operator. A configured-but-unreadable `caSecretRef`
(missing Secret, missing key, or contents with no PEM certificate in them)
is a hard error, never a silent fall-back to the platform roots.

<Callout type="warn">
  `spec.tls.insecureSkipVerify: true` turns certificate verification off
  entirely. It exists for throwaway/self-signed instances; prefer
  `caSecretRef`, which keeps verification on.
</Callout>

This block covers the **Authentik API connection only**. Vault has its own
CA setting — they are routinely signed by different CAs — see
[Trusting a private Vault CA](#trusting-a-private-vault-ca) below.

## Secret storage backend

`AuthentikApplication`'s oauth2 credentials (`AUTHENTIK_CLIENT_ID`/
`AUTHENTIK_CLIENT_SECRET`/`AUTHENTIK_URL`) are written to a Kubernetes
`Secret` by default — one per `AuthentikApplication` CR, same name/
namespace. To write them to Vault instead, set `spec.secretStore` on
the `AuthentikInstance`:

```yaml
apiVersion: authentik.weebo.io/v1alpha1
kind: AuthentikInstance
metadata:
  name: main
spec:
  url: https://login.example.com
  tokenSecretRef:
    name: authentik-api-token
    namespace: weebo-authentik
    key: token
  secretStore:
    backend: vault
    vault:
      address: https://vault.example.com:8200
      mount: secret
      pathPrefix: weebo-authentik # optional, this is the default
      kubernetesAuthRole: weebo-authentik-operator
      kubernetesAuthMount: kubernetes # optional, this is the default
```

The operator authenticates to Vault using its own ServiceAccount JWT
against Vault's Kubernetes auth backend (`kubernetesAuthRole` must
already be configured Vault-side to trust that ServiceAccount) — no
static Vault token to provision or rotate. Credentials land at
`<mount>/data/<pathPrefix>/<namespace>/<name>` (KV v2), one secret per
`AuthentikApplication` CR, same naming convention as the Kubernetes
backend.

The write is **conditional**: the operator reads the path first and only
issues a KV v2 `set` when the stored document differs from what it would
write. This matters because `set` always creates a new version, even for
an identical document, while the reconciler rewrites credentials on every
pass (a synced object requeues every 300s) — writing unconditionally would
churn a new Vault version per application every five minutes and roll the
KV version history over continuously. Drift is still corrected: a path
edited by hand no longer matches and is rewritten on the next pass. The
Kubernetes backend needs no equivalent, since re-applying an identical
`Secret` is already a no-op at the API server.

#### Trusting a private Vault CA

If Vault (or OpenBao) presents a TLS certificate signed by a private CA,
point `secretStore.vault.caSecretRef` at a Kubernetes `Secret` holding
the CA's PEM bundle. The operator reads it via the API and hands it to
its Vault client — no volume mount or `VAULT_CACERT` env wiring needed:

```yaml
  secretStore:
    backend: vault
    vault:
      address: https://openbao.example.com:8200
      mount: secret
      kubernetesAuthRole: weebo-authentik-operator
      caSecretRef:
        name: openbao-tls
        namespace: weebo-authentik
        key: ca.crt # must hold a PEM bundle
```

When `caSecretRef` is unset, the system trust store is used. A
configured-but-unreadable `caSecretRef` (missing Secret or key) is a hard
reconcile error, never a silent fall-back to the system roots. The
operator's ClusterRole already grants `get` on Secrets cluster-wide, so
the CA Secret may live in any namespace.

### Per-application targets and fan-out

The `secretStore` above defines a single default destination for every
application on the instance. An individual `AuthentikApplication` can
override that by listing explicit `spec.secretTargets`, each selecting a
`backend` and — for Vault — an exact `path`. When one or more targets
are set, the credentials are **written to every one of them**; Kubernetes
and Vault targets may be mixed, so a single application can populate two
Vault paths and a Kubernetes `Secret` at once:

```yaml
apiVersion: authentik.weebo.io/v1alpha1
kind: AuthentikApplication
metadata:
  name: frontend
  namespace: team-a
spec:
  instanceRef: main
  name: Frontend
  slug: frontend
  provider:
    kind: oauth2
    oauth2:
      authorizationFlow: default-authorization-flow
      invalidationFlow: default-invalidation-flow
  secretTargets:
    - backend: vault
      path: apps/frontend/oauth # exact KV path under the instance's mount
    - backend: vault
      path: shared/frontend-credentials
    - backend: kubernetes
      name: frontend-oauth # Secret name; defaults to the CR name
```

Notes:

- A `vault` target reuses the connection/auth config from the instance's
  `secretStore.vault` (address, mount, Kubernetes-auth role) — it only
  chooses _where under that mount_ to write. The instance must therefore
  carry a `secretStore.vault` block for Vault targets to work, even if the
  instance's default `backend` is `kubernetes`; otherwise the application
  reconcile errors.
- A `vault` target with no `path` falls back to the default
  `<pathPrefix>/<namespace>/<name>` convention; a `kubernetes` target with
  no `name` falls back to the application CR's name (in the application's
  own namespace).
- Leaving `secretTargets` empty (the default) keeps the historical
  behavior: the single destination from the instance's `secretStore`.
- `secretTargets` is only meaningful for `provider.kind: oauth2` — proxy
  providers carry no client secret, so it is ignored for them.

## Multiple instances

Only `AuthentikApplication` carries an explicit `instanceRef`.
`AuthentikGroup`/`AuthentikUser`/`AuthentikOutpost`/`AuthentikBrand`
have no such field — the operator resolves them against the single
`AuthentikInstance` CR in the cluster, and errors
(`AmbiguousDefaultInstance`) if there's more than one. Running against
more than one real Authentik instance at a time isn't supported yet for
those kinds.
