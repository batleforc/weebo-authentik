apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: authentikgroups.authentik.weebo.io
spec:
  group: authentik.weebo.io
  names:
    categories: []
    kind: AuthentikGroup
    plural: authentikgroups
    shortNames: []
    singular: authentikgroup
  scope: Cluster
  versions:
  - additionalPrinterColumns: []
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Auto-generated derived type for AuthentikGroupSpec via `CustomResource`
        properties:
          spec:
            description: |-
              Mirrors `authentik_group`. Cluster-scoped — group hierarchy is an
              org-wide identity concept, not owned by a namespace. Only reachable via
              cluster-scoped RBAC (no `AuthentikNamespacePolicy` governs this kind).
            properties:
              attributes:
                additionalProperties:
                  type: string
                default: {}
                type: object
              isSuperuser:
                default: false
                type: boolean
              name:
                type: string
              parentRef:
                description: |-
                  Name of another `AuthentikGroup`, for hierarchy (`weebo_user` ->
                  `weebo_moderator` -> `weebo_admin` in the Terraform module).
                nullable: true
                type: string
            required:
            - name
            type: object
          status:
            nullable: true
            properties:
              authentikId:
                description: |-
                  The Authentik-side primary key. Presence means this CR actually
                  owns/created the corresponding Authentik object — set once on
                  first successful create (or seeded by the import tool), never
                  inferred by name-matching. See `.prompt/plan.md`.
                nullable: true
                type: string
              conditions:
                default: []
                items:
                  properties:
                    lastTransitionTime:
                      description: Time is a wrapper around time.Time which supports correct marshaling to YAML and JSON.  Wrappers are provided for many of the factory methods that the time package offers.
                      format: date-time
                      nullable: true
                      type: string
                    message:
                      type: string
                    observedGeneration:
                      format: int64
                      nullable: true
                      type: integer
                    reason:
                      description: |-
                        One of `domain::error::ReasonCode`'s `as_str()` values — never a
                        hand-written string.
                      type: string
                    status:
                      description: '`"True"`, `"False"`, or `"Unknown"`.'
                      type: string
                    type:
                      type: string
                  required:
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              observedGeneration:
                format: int64
                nullable: true
                type: integer
            type: object
        required:
        - spec
        title: AuthentikGroup
        type: object
    served: true
    storage: true
    subresources:
      status: {}
